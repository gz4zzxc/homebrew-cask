name: Update Futu Niuniu Cask

on:
  schedule:
    - cron: "54 23 * * *"  # 北京时间 07:54
  workflow_dispatch: {}

permissions:
  contents: write
  actions: read      # 读取其他 workflow 状态

concurrency:
  group: update-futu-niuniu
  cancel-in-progress: true

jobs:
  update:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      # 检查是否有其他更新正在进行
      - name: Check for running updates
        id: check-running
        run: |
          echo "::group::Checking for running updates"
          RUNNING_COUNT=$(gh run list --workflow="Update Futu Niuniu Cask" --status=in_progress --limit=10 --json status --jq '.[] | select(.status == "in_progress") | .status' | wc -l)
          echo "Found $RUNNING_COUNT running updates"
          
          if [[ "$RUNNING_COUNT" -gt 1 ]]; then
            echo "::warning::Multiple updates are running, this might cause conflicts"
            echo "running_conflict=true" >> "$GITHUB_OUTPUT"
          else
            echo "running_conflict=false" >> "$GITHUB_OUTPUT"
          fi
          echo "::endgroup::"

      # 优化的下载缓存策略
      # 只缓存下载的 DMG 文件，避免重复下载相同版本
      # 使用 github.sha 作为缓存键，确保每次提交的缓存独立
      - name: Restore download cache
        uses: actions/cache/restore@v4
        id: download-cache
        with:
          path: /tmp/*.dmg
          key: ${{ runner.os }}-futu-niuniu-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-futu-niuniu-

      - name: Check new version
        id: check
        continue-on-error: true  # 允许失败但不终止整个工作流
        shell: bash
        run: |
          set -euo pipefail
          CURRENT_VERSION=$(sed -n 's/^  version "\(.*\)"/\1/p' Casks/futu-niuniu.rb)

          echo "::group::Checking for new version"
          echo "Current version: $CURRENT_VERSION"

          # 解析下载页中的直链文件名获取版本
          page=$(curl -fsSL 'https://www.futunn.com/download/mac') || page=""
          if [[ -z "$page" ]]; then
            page=$(curl -fsSL 'https://www.futunn.com/download') || true
          fi

          NEW_VERSION=$(printf "%s" "$page" | grep -Eo 'FTNN_desktop_[0-9]+(\.[0-9]+)+_Website\.dmg' | head -n1 | sed -E 's/.*FTNN_desktop_([0-9.]+)_Website\.dmg/\1/')

          if [[ -z "${NEW_VERSION:-}" ]]; then
            echo "::warning::Failed to parse Futu version, skipping."
            echo "update_needed=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          echo "Latest version: $NEW_VERSION"

          if [[ "$NEW_VERSION" == "$CURRENT_VERSION" ]]; then
            echo "::notice::Already at latest version: $CURRENT_VERSION"
            echo "update_needed=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          echo "::notice::New version available: $NEW_VERSION"
          echo "::endgroup::"

          URL="https://softwaredownload.futunn.com/FTNN_desktop_${NEW_VERSION}_Website.dmg"
          
          echo "::group::Downloading file"
          echo "Downloading from: $URL"
          
          curl --retry 5 --retry-all-errors --retry-delay 2 -fSL "$URL" -o /tmp/futu-niuniu.dmg
          
          # 验证下载的文件
          if [[ ! -f /tmp/futu-niuniu.dmg ]]; then
            echo "::error::Download failed - file not found"
            exit 1
          fi
          
          # 检查文件大小
          FILE_SIZE=$(stat -f%z /tmp/futu-niuniu.dmg 2>/dev/null || stat -c%s /tmp/futu-niuniu.dmg 2>/dev/null || echo "0")
          
          if [[ "$FILE_SIZE" -lt 1000000 ]]; then
            echo "::error::Downloaded file is too small - possible corruption"
            exit 1
          fi
          
          echo "::notice::File downloaded successfully - ${FILE_SIZE} bytes"
          echo "::endgroup::"

          SHA=$(shasum -a 256 /tmp/futu-niuniu.dmg | awk '{print $1}')

          echo "new_version=$NEW_VERSION" >> "$GITHUB_OUTPUT"
          echo "new_sha256=$SHA" >> "$GITHUB_OUTPUT"
          echo "update_needed=true" >> "$GITHUB_OUTPUT"
          echo "old_version=$CURRENT_VERSION" >> "$GITHUB_OUTPUT"

      # 验证检查步骤的输出
      - name: Validate check outputs
        if: steps.check.outputs.update_needed == 'true'
        run: |
          if [[ -z "${{ steps.check.outputs.new_version }}" ]]; then
            echo "::error::New version is empty"
            exit 1
          fi
          
          if [[ -z "${{ steps.check.outputs.new_sha256 }}" ]]; then
            echo "::error::SHA256 is empty"
            exit 1
          fi
          
          echo "::notice::All outputs validated successfully"

      # 保存下载缓存
      - name: Save download cache
        if: |
          always() && 
          steps.download-cache.outputs.cache-hit != 'true' &&
          steps.check.outputs.update_needed == 'true'
        uses: actions/cache/save@v4
        with:
          path: /tmp/*.dmg
          key: ${{ steps.download-cache.outputs.cache-primary-key }}

      - name: Apply updates
        if: steps.check.outputs.update_needed == 'true'
        env:
          NEW_VERSION: ${{ steps.check.outputs.new_version }}
          NEW_SHA256: ${{ steps.check.outputs.new_sha256 }}
        shell: bash
        run: |
          set -euo pipefail
          OLD_VERSION=$(sed -n 's/^  version "\(.*\)"/\1/p' Casks/futu-niuniu.rb)

          # 记录更新摘要
          echo "::group::Update Summary"
          echo "Cask: futu-niuniu"
          echo "Old Version: $OLD_VERSION"
          echo "New Version: $NEW_VERSION"
          echo "SHA256: $NEW_SHA256"
          echo "::endgroup::"

          sed -i '' "s/^  version \".*\"/  version \"$NEW_VERSION\"/" Casks/futu-niuniu.rb
          sed -i '' "s/^  sha256 \".*\"/  sha256 \"$NEW_SHA256\"/" Casks/futu-niuniu.rb

          sed -i '' "s/| \`futu-niuniu\` | [^|]* |/| \`futu-niuniu\` | $NEW_VERSION |/" README.md

          echo "::notice::Cask file updated successfully"
          echo "::notice::README.md updated successfully"

      - name: Validate
        if: steps.check.outputs.update_needed == 'true'
        shell: bash
        run: |
          ruby -c Casks/futu-niuniu.rb
          grep "\`futu-niuniu\`" README.md | cat

      - name: Commit and push
        if: steps.check.outputs.update_needed == 'true'
        env:
          NEW_VERSION: ${{ steps.check.outputs.new_version }}
        shell: bash
        run: |
          set -euo pipefail
          OLD_VERSION=$(sed -n 's/^  version "\(.*\)"/\1/p' Casks/futu-niuniu.rb)
          
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add Casks/futu-niuniu.rb README.md
          git commit \
            -m "chore: update Futu Niuniu to version ${NEW_VERSION}" \
            -m "- Updated version from ${OLD_VERSION} to ${NEW_VERSION}" \
            -m "- Updated SHA256 checksum" \
            -m "- Updated README.md version table" \
            -m "- Source: https://www.futunn.com/download/mac" || exit 0
          git push

      # 最终状态报告
      - name: Final status report
        if: always()
        run: |
          echo "::group::Final Status Report"
          echo "Workflow: Update Futu Niuniu Cask"
          echo "Run ID: ${{ github.run_id }}"
          echo "Run Attempt: ${{ github.run_attempt }}"
          echo "Status: ${{ job.status }}"
          
          if [[ "${{ steps.check.outputs.update_needed }}" == "true" ]]; then
            echo "Update: Required and processed"
            echo "Old Version: ${{ steps.check.outputs.old_version }}"
            echo "New Version: ${{ steps.check.outputs.new_version }}"
          else
            echo "Update: Not required or failed"
          fi
          
          if [[ "${{ steps.check-running.outputs.running_conflict }}" == "true" ]]; then
            echo "Conflict: Multiple updates detected"
          fi
          
          echo "::endgroup::"
          
          # 清理临时文件
          rm -f /tmp/*.dmg
          echo "::notice::Temporary files cleaned up"
